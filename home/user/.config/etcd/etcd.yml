# This is the configuration file for the etcd server.

# Human-readable name for this member.
#name: 'user'

# Path to the data directory.
#data-dir:

# Path to the dedicated wal directory.
#wal-dir:

# Number of committed transactions to trigger a snapshot to disk.
snapshot-count: 8192

# Time (in milliseconds) of a heartbeat interval.
heartbeat-interval: 128

# Time (in milliseconds) for an election to timeout.
election-timeout: 1024

# Raise alarms when backend size exceeds the given quota. 0 means use the
# default quota.
quota-backend-bytes: 0

# List of comma separated URLs to listen on for peer traffic.
listen-peer-urls: "http://127.0.0.1:2380,http://[::1]:2380"

# List of comma separated URLs to listen on for client traffic.
listen-client-urls: "http://127.0.0.1:2379,http://[::1]:2379"

# Maximum number of snapshot files to retain (0 is unlimited).
max-snapshots: 4

# Maximum number of wal files to retain (0 is unlimited).
max-wals: 4

# Comma-separated white list of origins for CORS (cross-origin resource sharing).
cors: http://127.0.0.1:2380,http://[::1]:2380,https://etcd.user.home.arpa:2380

# List of this member's peer URLs to advertise to the rest of the cluster.
initial-advertise-peer-urls: http://127.0.0.1:2380,http://[::1]:2380,https://etcd.user.home.arpa:2380

# List of this member's client URLs to advertise to the public.
advertise-client-urls: http://127.0.0.1:2379,http://[::1]:2379,https://etcd.user.home.arpa:2379

# For single-node bootstrap, map name to its peer URL.
initial-cluster: malwareslayer=http://127.0.0.1:2380,malwareslayer=http://[::1]:2380,malwareslayer=https://etcd.user.home.arpa:2379

# Initial cluster token for the etcd cluster during bootstrap.
initial-cluster-token: 'etcd-cluster'

# Initial cluster state ('new' or 'existing').
initial-cluster-state: 'new'

# Reject reconfiguration requests that would cause quorum loss.
strict-reconfig-check: true

# Enable runtime profiling data via HTTP server
enable-pprof: true

# Valid values include 'on', 'readonly', 'off'
proxy: 'off'

# client-transport-security:
#  cert-file:
#  key-file:
#  client-cert-auth: false
#  trusted-ca-file:
#  auto-tls: false

# peer-transport-security:
#  cert-file:
#  key-file:
#  client-cert-auth: false
#  trusted-ca-file:
#  auto-tls: false

self-signed-cert-validity: 1

log-level: debug
logger: zap
log-outputs: [stderr]

force-new-cluster: false

auto-compaction-mode: periodic
auto-compaction-retention: "1"

cipher-suites: [
  TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,
  TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
]

tls-min-version: 'TLS1.2'
tls-max-version: 'TLS1.3'
